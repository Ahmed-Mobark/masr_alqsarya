import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary - Golden Yellow from Figma
  static const Color primary = Color(0xFFFEDB65);
  static const Color primaryDark = Color(0xFFFBBC05);
  static const Color primaryDeep = Color(0xFFC09300);

  // Secondary
  static const Color secondary = Color(0xFF060C32);

  // Badge / Status accent
  static const Color statusBadge = Color(0xFFC89563);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFFFFFF);
  static const Color inputBg = Color(0xFFF5F5F5);
  static const Color scaffoldBg = Color(0xFFF5F5F5);

  // Text
  static const Color darkText = Color(0xFF000000);
  static const Color bodyText = Color(0xFF19181F);
  static const Color greyText = Color(0xFF777982);
  static const Color lightGreyText = Color(0xFF9C9C9C);
  static const Color captionText = Color(0xFF707070);

  // Neutrals
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Status
  static const Color error = Color(0xFFEB4335);
  static const Color success = Color(0xFF34A853);

  // Border
  static const Color border = Color(0xFFE2E2E2);
  static const Color divider = Color(0xFFC3C3C3);

  // Social colors
  static const Color google = Color(0xFFEB4335);
  static const Color apple = Color(0xFF000000);

  // Tab bar
  static const Color tabActive = Color(0xFFFBBC05);
  static const Color tabInactive = Color(0xFF777982);
}
